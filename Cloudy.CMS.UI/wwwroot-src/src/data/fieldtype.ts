type FieldType = {
  autoGenerate: boolean,
  description: string,
  label: string,
  listPartial: string,
  name: string,
  partial: string,
  renderChrome: boolean,
  settings: any,
  tab: string,
  validators: any,
}

export default FieldType;