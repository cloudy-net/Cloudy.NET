@page
@using Cloudy.CMS.UI.FormSupport
@model Cloudy.CMS.UI.Pages.Admin.NewModel

@await Html.PartialAsync("Shared/Nav")

<div class="container">
    <h1 class="h2">
        <a href="@Url.Page("/Admin/List", new { ContentType = Model.ContentType.Name })">@Model.ContentTypeName.PluralName</a>
        /
        New
        <a class="btn btn-sm btn-beta" href="@Url.Page("/Admin/List", new { ContentType = Model.ContentType.Name })">Back</a>
    </h1>

    @await Html.PartialAsync("Shared/Form", new FormViewModel())

    <div class="d-flex">
        <button class="btn btn-primary">Save</button>
    </div>
</div>
